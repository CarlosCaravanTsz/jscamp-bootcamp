<!-- Aquí irá el feedback del ejercicio -->
Excelente trabajo! Muy buenos tests, y muy bien escritos.

Modificamos el script en `package.json` para correr los tests con `NODE_ENV=test`.
Agregamos algunos handlers en `app.test.js` y cambios mínimos.
Modificamos el contrato con Zod para que sean más fieles al Schema de los Jobs.